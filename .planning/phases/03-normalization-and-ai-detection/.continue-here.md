---
context: phase
phase: 03-normalization-and-ai-detection
task: null
total_tasks: null
status: planning
last_updated: 2026-04-09T22:02:50.380Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

<current_state>
Phase 03 discuss-phase is complete — CONTEXT.md is written with all decisions locked.

gsd-plan-phase 03 was in progress when the session was closed. A planner agent was running in the background. Plan files (03-01-PLAN.md etc.) may or may not have been written to .planning/phases/03-normalization-and-ai-detection/.

**First action on resume:** Check if plan files exist. See <next_action>.
</current_state>

<completed_work>

- Phase 02 fully verified (4/4 score, VERIFICATION.md complete) — done before this session
- Phase 03 discuss-phase complete — 03-CONTEXT.md written with all implementation decisions
- gsd-plan-phase 03 initiated — planner agent spawned (outcome unknown at pause time)
</completed_work>

<remaining_work>

- Verify whether planner agent produced plan files
- If plans exist: run plan checker (/gsd-plan-phase 03 to trigger checker, or check manually)
- If no plans: re-run /gsd-plan-phase 03 --skip-research
- Execute Phase 03
</remaining_work>

<decisions_made>

- normalize(raw: dict) -> ProjectProfile lives in src/naz/detection/normalizer.py
- AI detection: hardcoded name-prefix list, no LLM, no network calls
- Broad AI package list: openai, anthropic, google-generativeai, cohere, mistralai, together, langchain*, langgraph, crewai, autogen*, agentops, llamaindex, llama-index*, chromadb, pinecone-client, weaviate-client, qdrant-client, faiss-cpu, transformers, huggingface-hub, sentence-transformers, diffusers, ollama, dspy-ai, instructor, haystack-ai, outlines, mem0ai, composio
- Tech categories: runtime, framework, database, infrastructure, devtools, ai_ml
- Unknown techs → "other" category, no errors, no data loss
- Lenient Specfy field handling — missing fields default to empty list/dict
- naz.detection.__init__ must re-export normalize alongside run_specfy
- v0.2 deferred: remote-maintained AI package list with local cache fallback
- Research skipped — phase is well-understood Python data transformation
</decisions_made>

<blockers>
- Planner agent outcome unknown: check .planning/phases/03-normalization-and-ai-detection/ for plan files on resume
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-normalization-and-ai-detection/03-CONTEXT.md` — all locked decisions for this phase
2. `src/naz/models.py` — existing ProjectProfile, Technology, Dependency models (normalizer produces these)
3. `src/naz/detection/runner.py` — returns raw dict that normalizer consumes
4. `tests/fixtures/specfy_flat.json` — Specfy --flat output shape

## Infrastructure State
- No running services
- Node.js required for Specfy (already verified working in Phase 02)
- uv for Python env: `uv run pytest` to run tests

<context>
Session covered: Phase 03 discuss-phase (all 4 gray areas discussed, decisions locked) → gsd-plan-phase 03 started. User needed to leave so session was paused while planner agent was mid-run. The planner had all context it needed — CONTEXT.md was complete.

The normalizer is straightforward: loop Specfy deps, check against AI list, bucket into ai_dependencies vs dependencies. Loop techs, check against category dict, bucket into Technology objects. Pass languages through directly. Wrap in ProjectProfile. Simple.
</context>

<next_action>
Start with: `ls .planning/phases/03-normalization-and-ai-detection/` — if 03-01-PLAN.md exists, run `/gsd-plan-phase 03` to trigger the plan checker. If no plan files, run `/gsd-plan-phase 03 --skip-research` to replan.
</next_action>
